import { Component } from '@angular/core';
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  title = 'game';
// @Input()
 

  // ngDoCheck() {
  //  console.log(this.width, this.height);
  // }
}
// this.initElement.nativeElement.lastChild.autofocus = true
    // console.log(Array.from(this.initElement.nativeElement.lastChild.children), "ol");
    // console.log(this.initElement.nativeElement.lastChild.children, "pl");
    // this.squareBox.findIndex((x:number) => this.randomNumberArray.includes(x))