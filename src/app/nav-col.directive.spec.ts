import { NavColDirective } from './nav-col.directive';

describe('NavColDirective', () => {
  it('should create an instance', () => {
    const directive = new NavColDirective();
    expect(directive).toBeTruthy();
  });
});
